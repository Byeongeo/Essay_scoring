"use client";

/**
 * 플로팅(드래그·닫기 가능) 원본 비교창 — 2차 확장.
 * *** 클릭 → bbox로 원본 영역 표시. 1단계에서는 자리만 잡아 둔다. (비활성)
 */
export default function FloatingCompare() {
  // 2차에서 구현: 드래그 이동, 닫기, maskedTokens.bbox 영역 크롭 표시
  return null;
}
